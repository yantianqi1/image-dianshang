# ImageForge 功能测试记录

测试时间：2026-05-20 23:40:00 CST

测试对象：`/Volumes/Fanxiang S500Pro/项目/imageforge`

接口地址池：

- `https://iai.iisbo.com/v1`
- `https://ai.iisbo.com/v1`
- `https://api2.opcl.cloud/v1`

## 结论

当前主要功能已回归可用，并新增同源 Node 后端、异步生图任务、SSE 任务事件和结构化诊断日志。测试中发现并修复 4 个问题：

1. 生图请求没有把 `size` / `quality` / `n` 作为顶层参数传给 newapi。
2. 风格复刻只上传了参考设计图，没有上传产品素材图。
3. 编辑页“遮罩图层”文案会误导用户以为支持像素级蒙版。
4. 静态前端直连 newapi 时，Docker 日志无法看到真实生图错误，且长请求容易受 CDN 超时影响。

## 功能清单

| 功能 | 状态 | 证据 |
| --- | --- | --- |
| 同源后端健康检查 | 可用 | `GET /api/health` |
| 异步生图任务 | 可用 | `POST /api/render-jobs` + `GET /api/render-jobs/{id}/events` |
| 结构化错误日志 | 可用 | `jobId` / `traceId` / `NO_IMAGE_DATA` / 响应摘要 |
| newapi 地址池自动匹配 | 可用 | 后端 `/api/connection-test` |
| 地址记忆与失败切换 | 可用 | `server/upstream/client.test.js` |
| API Key 保存与连接测试 | 可用 | `dogfood-output/browser-feature-results.json` |
| 文字生图 | 可用 | 请求包含 `size=1536x1024`、`quality=medium`、`n=1` |
| 商品图批量生成 | 可用 | 6 张拆成 `n=4` + `n=2` 两批，不丢图 |
| 风格复刻 | 可用 | 已同时上传参考设计图和产品素材图 |
| 服装基础套图 | 可用 | 已上传服装图并生成图片请求 |
| 服装模特试穿 UI 切换 | 可用 | 模特图上传区按模式显示/隐藏 |
| 图片精修 | 可用 | 已上传参考图并生成图片请求 |
| 图片编辑 | 可用 | 已上传原图和辅助参考图，并透传 `size` / `quality` / `n` |
| AI 润色 | 可用 | `gpt-5.4` 文本请求返回正常 |
| 反推提示词 | 可用 | 视觉消息请求可构造并写回输入框 |
| 案例专区 | 可用 | 案例卡片可加载 |
| 历史记录 | 可用 | 生成后可写入，清空后为空 |

## 真实上游验证

结果文件：`dogfood-output/api-smoke/results.json`

| 测试项 | 状态 | 结果 |
| --- | --- | --- |
| `/v1/models` | 通过 | 返回 35 个模型 |
| `gpt-5.4` 文本聊天 | 通过 | 返回 `OK` |
| `gpt-image-2` 文生图横图 | 通过 | 输出 `1536 x 1024` |
| `gpt-image-2` 带参考图编辑 | 通过 | 输出 `1254 x 1254` |

生成文件：

- `dogfood-output/api-smoke/txt2img_landscape.png`
- `dogfood-output/api-smoke/img_input_edit.png`
- `dogfood-output/screenshots/final-desktop.png`

## 修复记录

### 参数透传

修改 `app.js`：

- 新增 `buildImageChatBody()`，统一构造生图请求体。
- 新增 `imageCountBatches()`，按上游单次最多 4 张拆分批量请求。
- `generateImage()`、`generateNew()`、`editImage()` 现在都会传递：
  - `size`
  - `quality`
  - `n`

### 风格复刻素材

修改 `generateNew('style')` 的素材读取逻辑：

- 之前读取 `uploads['style']`，实际不存在。
- 现在读取 `uploads['style-prod']`，确保产品素材图会随参考设计图一起传给上游。

### 编辑辅助参考图

修改 `index.html` / `app.js`：

- 将“遮罩图层”改为“辅助参考图”。
- 明确提示当前上游不支持像素级蒙版。
- 请求 prompt 中明确说明该图片是软参考，不伪装成真实 `mask`。

### newapi 地址池

新增 Node 后端：

- 内置 `https://iai.iisbo.com/v1`、`https://ai.iisbo.com/v1`、`https://api2.opcl.cloud/v1` 三个地址。
- 首次保存密钥时探测 `/models`，命中后按密钥指纹保存在后端内存。
- 生图、编辑走 `/api/render-jobs` 异步任务，并通过 SSE 接收任务事件。
- AI 润色、反推提示词走 `/api/openai/chat-completions` 后端代理。
- 已记忆地址请求失败时，会继续尝试其他地址，并在成功后更新记忆。

### 诊断日志

新增 `server/`：

- `server/main.js`：同源静态资源和 API 服务。
- `server/render/service.js`：生图任务执行与失败分类。
- `server/images/extract.js`：上游图片响应解析和响应结构摘要。
- `server/jobs/store.js`：内存任务状态和 SSE 事件回放。
- `server/upstream/client.js`：newapi endpoint failover 和 HTTP 日志。

当上游返回 HTTP 200 但没有图片字段时，任务失败为 `NO_IMAGE_DATA`，错误会带 `jobId`、`traceId` 和响应摘要。线上可直接通过 `docker compose logs -f` 定位失败层。

## 验证命令

```bash
node --check api-client.js
node --check app.js
node --check gallery-cases.js
node --check server/main.js server/config.js server/http/router.js server/http/static.js server/upstream/client.js server/upstream/endpoints.js server/upstream/logging.js server/upstream/memory.js server/render/service.js server/jobs/store.js server/images/extract.js
node --test server/images/extract.test.js server/jobs/store.test.js server/render/service.test.js server/upstream/client.test.js
python3.13 -m py_compile qa/real_api_smoke.py
python ~/.codex/skills/webapp-testing/scripts/with_server.py --server "HOST=127.0.0.1 node server/main.js" --port 8080 -- bash qa/run_agent_browser_probe.sh http://127.0.0.1:8080 qa/payload_probe.js
python ~/.codex/skills/webapp-testing/scripts/with_server.py --server "HOST=127.0.0.1 node server/main.js" --port 8080 -- bash qa/run_agent_browser_probe.sh http://127.0.0.1:8080 qa/browser_feature_probe.js
IMAGEFORGE_API_KEY=... python3.13 qa/real_api_smoke.py
```

## 当前限制

当前链路没有真正的 OpenAI `mask` 参数。页面已改为“辅助参考图”语义：可以作为弱约束参考图使用，但不承诺像素级蒙版编辑。
